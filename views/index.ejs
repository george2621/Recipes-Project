<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-md-6">
        <img src="/img/hero-image.png" width="607" height="510" class="d-block mx-lg-auto img-fluid " loading="lazy"
            alt="cooking blog with NodeJS">
    </div>

    <div class="col-12 col-md-6">
        <h1 class="display-5 fw-blod mb-3">Huge selection of delicios recipe ideas</h1>
        <p class="lead">
            Explore our huge selection of delicious recipe ideas including; easy desserts, delicious vegan and
            vegetarian dinner ideas, gorgeous pasta
            recipes, quick bakes, family-friendly meals and gluten-free recipes.
        </p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-1g px-4 me-md-2">Explore Latest</a>
            <a href="/random-recipe" cLass="btn btn-outline-secondary btn-1g px-4 me-md-2">Show Random</a>
        </div>
    </div>
</div>

<!-- Categories-->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
    <% if(categories !='' ) { %>
        <% categories.forEach((category)=> { %>
            <a href="/categories/<%-category.name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="img/<%-category.image %> " alt="<%-category.image %>" Loading="lazy">
                </div>
                <div class="pt-1">
                    <%-category.name %>
                </div>
            </a>
            <% }) %>
                <% } else {%>
                    <p>No item found.</p>
                    <% } %>

                        <a href="/categories" class="col text-center category__link">
                            <div class="category__img shadow">
                                <img src="img/view-all.jpg" alt="View All" Loading="lazy">
                            </div>
                            <div class="pt-1">View ALL</div>
                        </a>

</div>
<!--End Categories-->


<!-- Latest Recipe-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">view More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-4 g-2 8-1g-3">

        <% if(food !='' ) { %>
            <% food.latestRecipe.forEach((recipe)=> { %>

                <a href="/recipe/<%-recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="img/<%-recipe.image %>" alt="<%-recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%-recipe.name %>
                    </div>
                </a>
                <% }) %>
                    <% } else {%>
                        <p>No item found.</p>
                        <% } %>
    </div>

</section>

<!--End Latest Recipe-->


<!-- Thai Recipe-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Thai Recipes</h2>
        <a href="/categories/Thai" class="ms-auto">view More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-4 g-2 8-1g-3">

        <% if(typeof food.thai !=='undefined' && food.thai.length> 0) { %>
            <% food.thai.forEach((recipe)=> { %>

                <a href="/recipe/<%-recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="img/<%-recipe.image %>" alt="<%-recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%-recipe.name %>
                    </div>
                </a>
                <% }) %>
                    <% } else {%>
                        <p>No item found.</p>
                        <% } %>
    </div>

</section>
<!-- End Thai Recipe-->

<!-- American Recipe-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>American Recipes</h2>
        <a href="/categories/Thai" class="ms-auto">view More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-4 g-2 8-1g-3">

        <% if(typeof food.american !=='undefined' && food.american.length> 0) { %>
            <% food.american.forEach((recipe)=> { %>

                <a href="/recipe/<%-recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="img/<%-recipe.image %>" alt="<%-recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%-recipe.name %>
                    </div>
                </a>
                <% }) %>
                    <% } else {%>
                        <p>No item found.</p>
                        <% } %>
    </div>

</section>
<!--End American Recipe-->

<section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish-recipe.png" class="d-block mx-auto mb-4 img-fluid" alt="Publish your recipe for FREE today"
        width="566" height="208" Loading="lazy">
    <h1 class="display-5 fw-bold">Publish your recipe for FREE today</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your Recipe in front of thousands of people for free.</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
        </div>
    </div>
</section>